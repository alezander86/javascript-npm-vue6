apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "javascript-npm-vue6.fullname" . }}-test-connection"
  labels:
    {{- include "javascript-npm-vue6.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "javascript-npm-vue6.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
